[build]
  # Sitio estático: no hay paso de build, se publica la carpeta tal cual.
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
